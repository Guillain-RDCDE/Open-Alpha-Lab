# Normalise line endings: LF in the repo, auto on checkout.
* text=auto eol=lf

# Treat notebooks as text but don't try to merge them.
*.ipynb text eol=lf merge=binary

# Binary assets — never touch.
*.png binary
*.parquet binary
*.pdf binary
