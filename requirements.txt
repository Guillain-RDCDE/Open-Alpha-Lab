# Dependency source of truth: pyproject.toml. This file is the one-line alias
# the README points at — it builds the full research environment: the quantlab
# engine (editable, with its runtime deps incl. duckdb), the studies'
# econometrics/ML stack ([studies] extra: scipy, statsmodels, arch,
# scikit-learn) and the dev/test tooling ([dev] extra: pytest, notebook tools).
-e .[studies,dev]

# optional, live trading template only (install manually):
# MetaTrader5>=5.0.45
