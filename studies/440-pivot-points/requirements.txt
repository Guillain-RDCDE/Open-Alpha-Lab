# Extra dependencies for Study 440 (Floor-Trader Pivot Points).
# Core scientific stack (numpy, pandas, matplotlib) is assumed present at the repo level.
yfinance>=0.2.40      # real 5-minute intraday bars (cache-first; network only on a cache miss)
pyarrow>=12.0         # parquet cache for the intraday tapes
nbformat>=5.9         # building the two narrative notebooks
jupyter>=1.0          # nbconvert --execute for the executed notebooks
