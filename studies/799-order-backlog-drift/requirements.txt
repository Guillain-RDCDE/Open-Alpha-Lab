# Study 799 — Order-Backlog Drift (RPO). Offline core needs only numpy + pandas.
# yfinance is used solely by data.fetch_panel() to (re)build the cache on a miss.
numpy>=1.24
pandas>=2.0
# --- cache (re)build + notebook execution only ---
yfinance>=0.2.40
matplotlib>=3.7
nbformat>=5.9
nbconvert>=7.0
jupyter>=1.0
pytest>=7.0
pyarrow>=12.0
