# Study checklist — Definition of Done

Copy this with the template. A study is **published** (and held to the standard) the moment its
row is added to the [root README](../../README.md) studies table. Before you add that row, tick
every box — the CI gate [`tools/check_study_completeness.py`](../../tools/check_study_completeness.py)
enforces the **bold** ones and will fail the build otherwise.

- [ ] **README front-card** — ~30 lines: verdict box (shields badges), *what-we-tested*
  paragraph, links to the two notebooks. Not a 7-beat document (the beats live in the notebooks).
- [ ] **Both notebooks present and _executed_** — `01_for_the_curious.ipynb` and
  `02_for_the_quants.ipynb`, generated by `notebooks/build_notebooks.py`, then re-run so every
  code cell has its outputs/figures embedded:
  ```bash
  cd notebooks
  python build_notebooks.py
  jupyter nbconvert --to notebook --execute --inplace 01_for_the_curious.ipynb 02_for_the_quants.ipynb
  ```
- [ ] **Tests wired into CI** — add `pytest -q studies/NN-your-idea/tests` to
  [`.github/workflows/tests.yml`](../../.github/workflows/tests.yml).
- [ ] **`docs/references.md`** — the literature map (the claim's source + the relevant papers +
  the shared method citations). `docs/` is never empty.
- [ ] **`docs/results.md`** (recommended) — the fingerprinted, as-of'd reproducible headline run
  (`quantlab/repro.py`).
- [ ] **Dual-track asides** — `02_for_the_quants` carries `> 💡 In plain words` notes; the
  curious notebook may carry `> 🔬 For the quants` notes.
- [ ] **Verdict rubric** — Signal `REAL`/`WEAK`/`NONE` + Tradability `INVESTABLE`/`FRAGILE`/`MIRAGE`
  (+ an optional 3rd myth-check axis), with the shared shields palette.
- [ ] **Deterministic & offline** — fixed seeds; the reproducible core and tests run with no
  network (live data only on an explicit cache miss).

Verify locally before publishing:
```bash
python tools/check_study_completeness.py
```

Full rationale: [`../../METHODOLOGY.md`](../../METHODOLOGY.md) → *Definition of Done*.
