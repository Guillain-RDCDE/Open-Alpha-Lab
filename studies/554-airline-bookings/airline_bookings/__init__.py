"""Study 554 — Airline-Bookings: travel-booking momentum as a leading indicator.

Synthetic-only alt-data study. See ``data.py`` (the deterministic generator + a cache-first
real-fetch stub that returns empty by design — no free booking tape exists) and ``strategy.py``
(the predictive regression, the placebo null, costs, robustness sweep and positive control).
"""
