"""Study 56 — Tide-Table: the Shiller CAPE actually forecasts long-run returns.

The cyclically-adjusted P/E (CAPE) predicts the next decade's equity returns — high CAPE, low returns
(Campbell & Shiller 1998). On 116 years it explains ~28% of 10-year-ahead real returns: a real,
valuable signal — and the thing the Fed Model (Study 47) only pretended to be. The catch is the
horizon: it sets a 10-year tide, it does not time next year.
"""

from . import data, strategy  # noqa: F401
